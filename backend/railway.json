{
  "$schema": "https://schema.railway.app/railway-schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "node src/models/migrate.js && node src/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
